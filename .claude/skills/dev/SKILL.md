---
name: dev
description: Start the Vite dev server and open browser preview
allowed-tools: Bash(npm run dev)
---

Start the development server:

1. Run `npm run dev` to start the Vite dev server on port 8080
2. Report the local URL to the user
3. If a preview tool is available, open `http://localhost:8080` in the preview
